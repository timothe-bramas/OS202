pour compiler avec #include <mpi.h>
on va compiler avec $mpicxx dans le make_linux.inc
puis executer avec mpirun -n nbp ... 

Trucs à faire pour le rapport : 

-Lister la configuration de la machine (lscpu)
-Trouver les endroits du code qui prennent le plus de temps
-Les paralléliser en priorité


Parallélisation affichage/calcul :
Il va falloir gérer les entrées clavier : keyboard evenement sur le processus affichage => prévient le process calcul avec un send => le process calcul 
